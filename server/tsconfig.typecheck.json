{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    // Kontrola typů se dívá i na testy a konfiguraci, které se nesestavují.
    "rootDir": "."
  },
  "include": ["src/**/*.ts", "vitest.config.ts"],
  "exclude": ["node_modules", "dist"]
}
